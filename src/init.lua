--!strict
local SchemeHandler = require(script.SchemeHandler)
local EventListener = require(script.EventListener)

local Tailblox = {
	Scheme = SchemeHandler,
	EventListener = EventListener,
}

return Tailblox
